const password=document.getElementById("password"),showPassword=document.getElementById("showPassword"),loginForm=document.getElementById("loginForm");if(showPassword)showPassword.addEventListener("click",()=>{const v=password.type==="text";password.type=v?"password":"text";showPassword.textContent=v?"Show":"Hide"});if(loginForm)loginForm.addEventListener("submit",e=>{e.preventDefault();window.location.href="dashboard.html"});